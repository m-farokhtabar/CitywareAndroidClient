<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="toolbarStyle">@style/Custom.Widget.Toolbar</item>

    </style>

    <style name="Custom.Widget.Toolbar" parent="Widget.AppCompat.Toolbar">
        <item name="contentInsetStart">0dp</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="SwitchCompatTheme">
        <item name="colorControlActivated">@color/ButtonBackgroundThemeColor</item>
    </style>

    <style name="SwitchCompatRed">
        <item name="colorControlActivated">@color/ButtonBackgroundRedColor</item>
    </style>

    <style name="RatingBar" parent="Theme.AppCompat">
        <item name="colorControlNormal">@color/BackgroundThemeLightColor</item>
        <item name="colorControlActivated">@color/BackgroundThemeColor</item>
    </style>

    <style name="SwitchThemeSetting" parent="Theme.AppCompat.Light">
        <item name="colorControlActivated">@color/ButtonBackgroundThemeColor</item>
        <item name="colorSwitchThumbNormal">@color/ButtonBackgroundGreenColor</item>
        <item name="android:colorForeground">@color/ButtonBackgroundPressGreenColor</item>
    </style>
</resources>
